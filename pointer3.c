#include <Stdio.h>
#include<stdlib.h>

void main()


/*1. Dynamically allocates memory for N integers using malloc.

  2. Reads N integers from the user and stores them in the allocated memory using pointer arithmetic (not array indexing).

  3. Finds the maximum and minimum values using only pointers.

  4. Prints all values, max, and min.

  5. Frees the allocated memory.*/

{
int a;

printf("Enter num N:")
scanf("%d",&a);
}